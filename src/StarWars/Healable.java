package StarWars;
/**
 * Healable Interface which forces all classes that implements this class to have
 * the method heal
 * @author KevinD
 *
 */
public interface Healable {
	public void heal(int hp);
}
